# Server Configuration
server.port=8080

# Spring Application
spring.application.name=infosys-clinical-operations-platform

# MySQL Database Configuration
# You can set the MYSQL_PASSWORD environment variable or change the default below
spring.datasource.url=jdbc:mysql://localhost:3306/infosys_clinical_operations?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Program_20!25
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false

# JWT Configuration
# 256-bit secret key (Base64 encoded)
app.jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
app.jwt.expiration-ms=86400000

# CORS Allowed Origins
app.cors.allowed-origins=http://localhost:5173,http://127.0.0.1:5173


// run backend : .\run_backend.bat
// run frontend : cd frontend , npm run dev